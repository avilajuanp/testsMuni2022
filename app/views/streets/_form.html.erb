<%= form_with(model: street) do |form| %>
  <% if street.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(street.errors.count, "error") %> prohibited this street from being saved:</h2>

      <ul>
        <% street.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :Nombre, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :"Código de calle", style: "display: block" %>
    <%= form.text_field :street_code %>
  </div>

  <div>
    <%= form.label :Barrio, style: "display: block" %>
    <%#= form.number_field :barrio_id %>
    <%= form.select(:barrio_id, options_for_select(@barrios.map{|barrio| [barrio.name, barrio.id]}), 
    {:include_blank => "seleccione barrio"}, class:'form-control', required:true) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
